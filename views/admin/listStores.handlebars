<p></p>
<p class="h1 text-center">Stores Management <a type = "button" href="/admin/registerStore" class="btn btn-primary">Add Store</a></p> 
<hr />

{{#if stores}}
<div class="container">
	<div class="row">
		{{#each stores}}
		<div class="col">
			<div class="card" style="width: 18rem; margin: auto; ">
				<img src="{{posterURL}}" class="card-img-top" alt="{{name}}" style="width: 100%; height: 10vw; object-fit: cover;">
				<div class="card-body">
					<h5 class="card-title">{{name}}</h5>
					<p class="card-text">{{description}}</p>
					<a href="/admin/editStore/{{id}}" class="btn btn-primary"><i class="fas fa-pencil-alt"></i> Edit</a>
					<a href="/admin/deleteStore/{{id}}" class="btn btn-danger" data-toggle="confirmation" data-title="Confirm delete?"><i class="far fa-trash-alt"></i>
						Delete
					</a>
				</div>
			</div>
		</div>



		{{!-- <div class="card border-primary w-75 ml-auto mb-3">
			<div class="card-header">
				<h5 class="card-title">{{name}}</h5>
			</div>
			<div class="card-body">
				<div class="row">
					<div class="col-lg-2">
						<a href="{{posterURL}}" data-title="{{name}}" data-toggle="lightbox">
							<img src="{{posterURL}}" alt="Poster" class="img-thumbnail img-fluid mb-2"
								onerror="this.src='/img/no-image.jpg'" style="max-height:160px">
						</a>
					</div>
					<div class="col-lg-8">
						<div class="row">
							<div class="col-sm-12 col-md-12 col-lg-3">
								<p class="font-italic">Category</p>
								<p class="text-info">{{category}}</p>
							</div>
						</div>
					</div>
					<div class="col-lg-2">
						<p>
							<a href="/admin/editStore/{{id}}" class="card-link mb-2">
								<i class="fas fa-pencil-alt"></i>
								Edit
							</a>
						</p>
						<p>
							<a href="/admin/deleteStore/{{id}}" class="card-link" data-toggle="confirmation"
								data-title="Confirm delete?">
								<i class="far fa-trash-alt"></i>
								Delete
							</a>
						</p>
					</div>
				</div>
			</div>
		</div> --}}

		{{/each}}
	</div>
	{{else}}
	<h3>No Store Available</h3>
	{{/if}}
</div>

<script>
	$('[data-toggle=confirmation]').confirmation({
		rootSelector: '[data-toggle=confirmation]',
	});
</script>